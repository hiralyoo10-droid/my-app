import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fbc3e8ff",
  },
  content: {
    flexDirection: 'row',
    flexWrap: "wrap",
    justifyContent: 'center',
    marginTop: 20,
    marginBottom: 20,
    gap: 30,
  },
});

export default styles;
